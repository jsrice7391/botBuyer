var assert = require('assert');
describe('Array', function () {
    describe('#indexOf()', function () {
        it('should return the value of the index', function () {
            assert.equal([1, 2, 3].indexOf(1), 0);
        });
    });
});
